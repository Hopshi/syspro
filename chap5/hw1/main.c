#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <fcntl.h>


int main() {
	char savedText[10][100];
	read(fd, &buf, 1);
	saveText[0][0] = buf;
	read(fd, &buf, 1);
	saveText[0][1] = buf;
}
